{
  "name": "@capture-bridge/cli",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nathanvale/capture-bridge.git",
    "directory": "packages/cli"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "@capture-bridge/source": {
        "types": "./src/index.ts",
        "import": "./src/index.ts"
      },
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf dist .tsbuildinfo",
    "dev": "concurrently \"pnpm:dev:*\" --names \"build,test\" --prefix-colors \"blue,green\"",
    "dev:build": "tsup --watch",
    "dev:test": "vitest watch",
    "lint": "eslint . --cache --cache-location ../../.eslintcache/cli",
    "lint:fix": "eslint . --fix --cache --cache-location ../../.eslintcache/cli",
    "prepublishOnly": "pnpm run build && test -d dist",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:ci": "vitest run --bail 1",
    "test:coverage": "vitest run --coverage",
    "test:integration": "TEST_MODE=integration vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@capture-bridge/foundation": "workspace:*",
    "@capture-bridge/storage": "workspace:*",
    "@capture-bridge/capture": "workspace:*",
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.1",
    "enquirer": "^2.4.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@capture-bridge/build-config": "workspace:*",
    "@orchestr8/testkit": "^2.2.0",
    "@types/node": "^24.3.0",
    "better-sqlite3": "^12.4.1",
    "tsup": "^8.3.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "private": true
}
